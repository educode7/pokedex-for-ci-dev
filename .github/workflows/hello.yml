name: Say hello

on:
  push:
    branches:
      - main

jobs:
  hello_world:
    runs-on: ubuntu-18.04
    steps:
      - name: Echo the message
        run: |
          echo "Hola educode7!"
      - name: Dime la fecha de peru
        run: date
      - run: ls -l